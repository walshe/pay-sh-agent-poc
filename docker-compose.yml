services:
  upstream:
    build: ./upstream
    networks:
      - paysh-net

  gateway:
    build: ./gateway
    ports:
      - "1402:1402"
    depends_on:
      - upstream
    networks:
      - paysh-net

networks:
  paysh-net:
    driver: bridge
